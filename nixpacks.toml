[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python init_db.py && python add_global_geography.py && gunicorn app:app --bind 0.0.0.0:$PORT"
