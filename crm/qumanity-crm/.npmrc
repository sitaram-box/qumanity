# NocoBase plugins (e.g. plugin-collection-sql) declare stale 0.x peer deps.
# legacy-peer-deps avoids ERESOLVE failures during npm install.
legacy-peer-deps=true
strict-peer-deps=false
auto-install-peers=false

# Prefer stable resolution over strict peer matching
fund=false
audit=false
