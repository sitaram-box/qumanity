{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "python init_db.py && python add_global_geography.py && gunicorn app:app --bind 0.0.0.0:${PORT:-8080}",
    "restartPolicyType": "ON_FAILURE"
  }
}
